{
  "properties": {
    "validateRequest": {
      "type": "boolean",
      "default": true,
      "description": "Set to true to validate the given request with the path schema parameters and requestBody"
    },
    "validateResponse": {
      "type": "boolean",
      "default": true,
      "description": "Set to true to validate the given response with the path schema response body"
    }
  },
  "definitions": {
    "properties": {
      "operation": {
        "properties": {
          "validateRequest": {
            "type": "boolean",
            "default": true,
            "description": "Set to true to validate the given request with the path schema parameters and requestBody"
          },
          "validateResponse": {
            "type": "boolean",
            "default": true,
            "description": "Set to true to validate the given response with the path schema response body"
          }
        }
      }
    }
  }
}
